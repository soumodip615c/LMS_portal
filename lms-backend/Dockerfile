# STUB — implemented in Phase 21 (Docker).
# Placeholder kept here now only so the folder structure matches
# techstack_arc_for_lms.docx. Actual multi-stage build, base image,
# and CMD will be added when we reach Phase 21.
