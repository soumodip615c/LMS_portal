"""
Database connection setup.

STUB — implemented in Phase 2 (Database).
Will contain: SQLAlchemy engine, SessionLocal, Base, and get_db()
dependency, connecting FastAPI to PostgreSQL via psycopg2.

hello world"""